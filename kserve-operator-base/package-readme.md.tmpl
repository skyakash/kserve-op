# KServe Operator Installation Package

## ⚡ Quick Start (TL;DR)

```bash
# 1. Install OLM (once per cluster — skip if already installed)
operator-sdk olm install && kubectl get pods -n olm

# 2. Set up pull credentials (only if images are on a private/rate-limited registry)
bash setup-credentials.sh   # generated with your credentials — skip if not present

# 3. Deploy the operator via OLM bundle
operator-sdk run bundle <your-bundle-image> --pull-secret-name docker-pull-secret
# The operator auto-creates the KServeRawMode CR — KServe installation begins immediately!

# 4. Watch progress (Ready in ~60s)
kubectl get kserverawmode -A -w

# 5. Deploy and test the Iris model
kubectl apply -f 06-sample-model/sklearn-iris.yaml
kubectl get isvc sklearn-iris -w   # wait for READY=True, then:
kubectl run --rm -i curl-test --image=curlimages/curl --restart=Never -- \
  curl -s -H "Content-Type: application/json" \
  -d '{"instances":[[6.8,2.8,4.8,1.4]]}' \
  http://sklearn-iris-predictor.default.svc.cluster.local/v1/models/sklearn-iris:predict
# Expected: {"predictions":[1]}
```

> **Alternative (no OLM):** Skip step 1. Instead of step 3, run `kubectl apply -f operator-deployment.yaml` to deploy the operator directly.

---

## Overview
This package contains the fully compiled deployment manifests required to install KServe automatically on your cluster using a custom, lightweight Operator. It operates entirely independently of Knative or Istio, utilizing Kubernetes native Deployments for inference serving workloads.

## Prerequisites
- A functional Kubernetes Cluster (v1.24+ recommended).
- `kubectl` installed and configured to access your cluster with ClusterAdmin privileges.


## Handling Private Registries

If the operator image is hosted on a private registry (e.g. Docker Hub with auth), a pull secret must exist in a few namespaces before the operator can pull its own controller image.

**Namespace lifecycle — Design C says we own exactly 2 namespaces, regardless of deploy path.**

**Namespaces we own:**

| Namespace | Created by | Purpose |
|---|---|---|
| `default` | Kubernetes (always exists) | Sample workloads (e.g. iris ISVC) |
| `cert-manager` | User (cluster prerequisite — `kubectl apply -f cert-manager.yaml`) | cert-manager webhook + controller (pre-install) |
| `kserve-operator-system` (default; configurable) | User OR `kubectl apply -f operator-deployment.yaml` | Operator pod home; OLM CatalogSource pod (Option A only) |
| `<KServe ns>` (default `kserve`; configurable) | User (before deploy) | KServe runtime (controllers, webhooks, ConfigMap); `KServeRawMode` CR lives here (Design C co-location) |

**OLM Infrastructure (Option A only — not ours):**

| Namespace | Notes |
|---|---|
| `olm` | OLM controller, catalog-operator, packageserver. Created by `operator-sdk olm install`. Pre-exists regardless of our operator. We never touch it. |
| `operators` | Default global-operators OperatorGroup. Created by `operator-sdk olm install`. We don't use it. |

See [`extra-docs/architecture-namespaces.md` § 9](../extra-docs/architecture-namespaces.md#9-design-c-footprint-always-2-namespaces-of-ours) for the full Design C rationale (why exactly 2 namespaces, why OLM's are infrastructure).

### Using `setup-credentials.sh` (if provided in this package)

If this package was generated with `--docker-username` and `--docker-password`, a `setup-credentials.sh` script was created with your credentials baked in. Run it **once on your cluster** at the right moment:

**For direct manifest deploy (Option B):**
```bash
kubectl apply -f operator-deployment.yaml   # creates kserve-operator-system namespace
bash setup-credentials.sh                   # creates pull secret in default + kserve-operator-system
# Operator auto-creates KServeRawMode CR — installation begins automatically
```

**For OLM bundle deploy (Option A):**
```bash
operator-sdk olm install                    # creates olm + operators namespaces (OLM infrastructure — not ours)
bash setup-credentials.sh                   # creates pull secret in default + kserve-operator-system
operator-sdk run bundle <your-bundle-image> --pull-secret-name docker-pull-secret
# Operator auto-creates KServeRawMode CR — installation begins automatically
```

> Both deploy paths use the same 2-namespace pull-secret footprint (`default` + the operator's home namespace). OLM's own namespaces (`olm`, `operators`) don't need our pull secret — OLM uses its own catalog auth for everything it does.

### Manual pull secret creation (without setup-credentials.sh)

If `setup-credentials.sh` was not generated, create the secret manually (Design C — 2 namespaces only):
```bash
for ns in default kserve-operator-system; do
  kubectl create secret docker-registry docker-pull-secret \
    --docker-server=<registry> \
    --docker-username=<user> \
    --docker-password=<token> \
    --namespace=$ns \
    --dry-run=client -o yaml | kubectl apply -f -
done
```

> If you customized the operator's home namespace (via `--operator-namespace` at build time OR `OPERATOR_NS`/`SYSTEM_NS` at deploy time — see "Customizing namespaces" below), substitute `kserve-operator-system` with your chosen value.


## Deployment Instructions

### Step 1: Deploy the Operator Controller

Choose **one** of the following methods to deploy the operator into your cluster.

#### Option A: Manual Manifests (Standard)
Deploy the operator using the pre-compiled Kubernetes manifests. This will create the `kserve-operator-system` namespace and start the controller.
```bash
kubectl apply -f operator-deployment.yaml
```

#### Option B: OLM Bundle (Enterprise Recommended)
If you have Operator Lifecycle Manager installed, you can deploy the operator as a bundle. This manages upgrades and dependencies automatically.

> [!IMPORTANT]
> OLM must be pre-installed. If not present: `operator-sdk olm install`

> [!TIP]
> The bundle image is built with `--provenance=false --sbom=false` to produce a flat single-manifest image that OLM can pull on both `linux/amd64` and `linux/arm64` hosts without platform resolution errors.

```bash
# Run the bundle. --install-mode auto-creates the OperatorGroup pointed at
# your KServe namespace (default 'kserve' — change to anything you want, e.g.
# SingleNamespace=my-kserve, and the operator's apply-time rewrite installs
# KServe there). Pre-create both namespaces (kserve-operator-system + the
# KServe target ns) before running.
operator-sdk run bundle <your-bundle-image-tag> \
  --namespace kserve-operator-system \
  --install-mode SingleNamespace=kserve \
  --pull-secret-name docker-pull-secret   # omit if image is public
```

---

### Customizing namespaces (Design C: both are configurable)

Both Design C namespaces (operator's home + KServe target) can be customized — at build time OR at deploy time:

**Build-time** (bakes the value into all shipped artifacts; useful for orgs with naming conventions):
```bash
./generate-kserve-operator.sh ... --operator-namespace=<your-operator-ns>
# Bakes <your-operator-ns> into operator-deployment.yaml + setup-credentials.sh + deploy-bundle.sh
```

**Deploy-time** (no rebuild; pick at apply time):

| Script | Env var | Purpose |
|---|---|---|
| `install-operator-deployment.sh` *(Option B wrapper)* | `OPERATOR_NAMESPACE=<ns>` | Operator lands in `<ns>` instead of the baked default. Python YAML walk rewrites every namespace ref in `operator-deployment.yaml` before `kubectl apply -f -`. |
| `install-operator-deployment.sh` *(Option B wrapper)* | `KSERVE_NS=<ns>` | KServe runtime + CR land in `<ns>` instead of `kserve`. Rewrites the Deployment's `WATCH_NAMESPACE` env var from OLM-downward-API source to a literal value. |
| `deploy-bundle.sh` *(Option A wrapper)* | `OPERATOR_NS=<ns>` | Same intent for OLM path; `operator-sdk run bundle --namespace=<ns>` handles substitution at install time. |
| `setup-credentials.sh` | `SYSTEM_NS=<ns>` | Tells the script where to create the operator pull secret (defaults to baked-in value). |

**Combined invocation (Option B example):**
```bash
OPERATOR_NAMESPACE=ops-custom KSERVE_NS=servewell \
    bash install-operator-deployment.sh
# Operator pod lands in 'ops-custom'; KServeRawMode CR + KServe runtime in 'servewell'.
# One generated package supports any namespace topology.
```

See [`extra-docs/architecture-namespaces.md` § 9](../extra-docs/architecture-namespaces.md#9-design-c-footprint-always-2-namespaces-of-ours-both-user-configurable) for the full design rationale (why these 4 override mechanisms; how they interact).

---

### Step 2: Watch KServe Auto-Installation

The operator automatically creates a default `KServeRawMode` CR on startup, in the namespace dictated by the OperatorGroup's `targetNamespaces`. KServe components (CRDs, RBAC, Core Controllers, Runtimes) are then installed into that same namespace via apply-time YAML rewriting. cert-manager is **not** installed by the operator — it is a cluster prerequisite.

No manual CR creation is needed.

---

### Step 3: Monitor Install Progress

The operator progresses through granular phases — no manual `sleep` commands needed. Watch in real time:

```bash
kubectl get kserverawmode -A -w
```

Expected output (using default `kserve` namespace):
```
NAMESPACE   NAME             PHASE                   AGE
kserve      kserve-rawmode   ValidatingCertManager   2s
kserve      kserve-rawmode   InstallingCRDs          8s
kserve      kserve-rawmode   InstallingRBAC          10s
kserve      kserve-rawmode   InstallingCore          11s
kserve      kserve-rawmode   InstallingRuntimes      38s
kserve      kserve-rawmode   Ready                   43s
```

Once `Ready`, verify all KServe system components are up:
```bash
kubectl get pods -n kserve
kubectl get crd | grep kserve
```

## Verification (Iris Inference)

Once the `kserve` namespace has populated and all controller manager pods indicate they are Ready, you can verify inference capabilities:

1. **Deploy Sample Model**:
   ```bash
   kubectl apply -f 06-sample-model/sklearn-iris.yaml
   ```

2. **Wait for Predictor Check**:
   ```bash
   kubectl get pods -l serving.kserve.io/inferenceservice=sklearn-iris
   ```
   Wait until the `sklearn-iris-predictor-<hash>` pod is `Running (1/1)`.

3. **Test Inference**: Either port-forward or use an in-cluster curl pod:
   ```bash
   # Option A: port-forward
   kubectl port-forward svc/sklearn-iris-predictor 8080:80 &
   curl -s -X POST -H "Content-Type: application/json" \
     -d @06-sample-model/iris-input.json \
     "http://localhost:8080/v1/models/sklearn-iris:predict"

   # Option B: in-cluster (no port-forward needed)
   kubectl run --rm -i curl-test --image=curlimages/curl --restart=Never -- \
     curl -s -H "Content-Type: application/json" \
     -d '{"instances":[[6.8,2.8,4.8,1.4]]}' \
     http://sklearn-iris-predictor.default.svc.cluster.local/v1/models/sklearn-iris:predict
   ```

   **Expected Result**: `{"predictions":[1]}`

---

## Alternative: Air-Gapped / Offline Model Test

If your cluster cannot reach `gs://` (Google Cloud Storage) to download model weights, the standard `sklearn-iris` test above will fail. Use this PVC-based offline alternative instead.

### 1. Create a local PersistentVolumeClaim
```bash
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: offline-models-pvc
spec:
  accessModes: [ReadWriteOnce]
  resources: { requests: { storage: 1Gi } }
EOF
```

### 2. Download and side-load the model into the PVC
*(On a machine with internet access)*
```bash
curl -sL https://storage.googleapis.com/kfserving-examples/models/sklearn/1.0/model/model.joblib -o model.joblib

# Mount the PVC to a temporary pod
kubectl run model-loader --image=busybox --restart=Never --overrides='{"spec":{"volumes":[{"name":"v","persistentVolumeClaim":{"claimName":"offline-models-pvc"}}],"containers":[{"name":"model-loader","image":"busybox","command":["sleep","3600"],"volumeMounts":[{"name":"v","mountPath":"/mnt/pvc"}]}]}}'

# Copy the file inside
kubectl wait --for=condition=Ready pod/model-loader
kubectl exec model-loader -- mkdir -p /mnt/pvc/sklearn/iris/1.0/model
kubectl cp model.joblib default/model-loader:/mnt/pvc/sklearn/iris/1.0/model/model.joblib
kubectl delete pod model-loader
```

### 3. Deploy the InferenceService using the PVC
```bash
cat <<EOF | kubectl apply -f -
apiVersion: "serving.kserve.io/v1beta1"
kind: "InferenceService"
metadata:
  name: "sklearn-iris-pvc"
  annotations:
    serving.kserve.io/deploymentMode: "RawDeployment"
spec:
  predictor:
    sklearn:
      storageUri: "pvc://offline-models-pvc/sklearn/iris/1.0/model"
EOF
```

### 4. Test offline inference
```bash
kubectl get isvc sklearn-iris-pvc -w   # wait for READY=True

kubectl run --rm -i curl-test-offline --image=curlimages/curl --restart=Never -- \
  curl -s -H "Content-Type: application/json" \
  -d '{"instances":[[6.8,2.8,4.8,1.4]]}' \
  http://sklearn-iris-pvc-predictor.default.svc.cluster.local/v1/models/sklearn-iris-pvc:predict
```
**Expected Result**: `{"predictions":[1]}`
