# KServe v0.18.0 e2e test report — `feat/kserve-0.18`

**Status:** ⏳ **IN PROGRESS — round started 2026-07-11 on Sonnet-5.** 26-test scope = full 25-test parity with the closed-out v0.16/v0.17 rounds (T01–T12, T17, T19–T22b, T25, T27–T32) **plus new T33** (PYTHONPATH blocked-env-var validator guard, the one genuinely-new upstream behavior in v0.18). Per-test results fill in one commit at a time during the execution round. T26 deferred (needs a podman host, same as v0.16/v0.17).

**Cross-reference:** recipes inherit from `extra-docs/0.17-test-report.md` (which itself references `0.16-test-report.md` for the canonical per-test recipes — a two-level historical anchor). This report records only v0.18 results and any deltas. Only the image tag differs from the v0.17 round: `v18001`/`v18002` instead of `v17003`/`v17004`, and the storage-initializer + kserve-controller pins are `:v0.18.0`.

## What changed v0.17.0 → v0.18.0 (context for this round)

Pure parity bump — the cleanest so far. Phase 1 drift re-check (fresh v0.18 extraction vs preserved v0.17 source) found:

- `config/default/` **byte-identical** → extraction entrypoint unchanged, no generator algorithm changes.
- **One new ConfigMap key** `disableHTTPRouteTimeout` (default `false`) in the ingress block. Our ConfigMap patch is parse-modify-reserialize, so it flows through untouched — verified present in regenerated `p-kserve-raw/`.
- `config/rbac/role.yaml` gains `localmodelnamespacecaches` inside the **kept** `kserve-manager-role` — survives as a harmless get/list/watch grant on an uninstalled CRD (RBAC doesn't validate resource existence). Zero functional impact.
- `kube-rbac-proxy:v0.18.0` unchanged from v0.17; controller image name unchanged (only pin tag flips); DROP filter still catches everything (config/crd builds the identical 6 core CRDs).
- **NEW admission validator (T33):** `pkg/validation/env_policy.go`, `DefaultBlockedEnvVars = ["PYTHONPATH"]`. Rejects any container (across predictor / transformer / explainer / WorkerSpec / InitContainers on `InferenceService`, and `ServingRuntime`/`ClusterServingRuntime`) that sets `PYTHONPATH`, with `setting PYTHONPATH in container "X" is not allowed for security reasons`. Same shape of "new validator breaks charts written against the older version" as the v0.15→v0.16 scaleMetric break — hence T33 + a Part D Troubleshooting entry (Phase 6). No impact on our shipped `sklearn-iris.yaml` sample (it sets no `PYTHONPATH`).

Retarget commit: `813c310 chore(generator): retarget extraction at KServe v0.18.0`.

## Summary table (round pending)

Images (to be built in Phase 4): `docker.io/akashneha/kserve-raw-operator:v18001` + `:v18001-bundle` (default flow), `:v18002` + `:v18002-bundle` (customer-registry → `akashdeohuf`). Cluster reset before every cluster test, verified via node `creationTimestamp`.

| Test | Type | Result | Recipe source |
|---|---|---|---|
| **T01** OLM bundle, default `kserve` ns | cluster | ✅ PASS (2026-07-11) | `0.16-test-report.md` §T01 |
| **T02** `install.sh` non-OLM default | cluster | ✅ PASS (2026-07-11) | §T02 |
| **T03** `install.sh` `KSERVE_NAMESPACE=servewell` | cluster | ✅ PASS (2026-07-11) | §T03 |
| **T04** direct `kubectl apply` (no OLM, no install.sh) | cluster | ✅ PASS (2026-07-12) | §T04 |
| **T05** OLM custom `servewell` (apply.go rewrite) | cluster | ✅ PASS (2026-07-12) | §T05 |
| **T06** OLM `servewell` + external URL (nginx-ingress) | cluster | ✅ PASS (2026-07-12) | §T06 |
| **T07** PVC offline iris | cluster | ✅ PASS (2026-07-12) | §T07 |
| **T08** `--cert` build-time CA injection | no-cluster | ✅ PASS (2026-07-12) | §T08 |
| **T09** `--cert` + `--customer-registry` combined | no-cluster | ✅ PASS (2026-07-12) | §T09 |
| **T10** customer-registry archive+load → `akashdeohuf` | cluster | ✅ PASS (2026-07-12) | §T10 (+ Docker Hub blob-mount finding) |
| **T11** customer-registry + `deploy-bundle.sh` | cluster | ✅ PASS (2026-07-12) | §T11 |
| **T12** customer-registry + `servewell` + external URL (triple-stack) | cluster | ✅ PASS (2026-07-12) | §T12 |
| **T17** CR delete UX | cluster | ✅ PASS (2026-07-12) | §T17 |
| **T19** `install-operator-deployment.sh` default + Design D iris | cluster | ✅ PASS (2026-07-12) | §T19 |
| **T20** 3-way flagship (`ops-custom`+`servewell`+`team-fraud`) | cluster | ✅ PASS (2026-07-12) | §T20 |
| **T21** indefinite auto-init retry | cluster | ✅ PASS (2026-07-12) | §T21 |
| **T22a** `setup-credentials.sh` 1-ns footprint | cluster | ✅ PASS (2026-07-12) | §T22a |
| **T22b** `setup-credentials.sh` multi-ns + typo fail-fast | cluster | ✅ PASS (2026-07-12) | §T22b |
| **T25** filter smoke (no cluster) | no-cluster | ✅ PASS (Phase 2) | §T25 — 0 dropped-CRD leaks, 6 core CRDs, 1 Deployment on regenerated v0.18 `p-kserve-raw/` |
| **T27** combined `--cert`+proxy Dockerfile patching | no-cluster | ✅ PASS (2026-07-12) | §T27 |
| **T28** proxy flag all-or-nothing validation | no-cluster | ✅ PASS (2026-07-12) | §T28 |
| **T29** bundle naming derivation | no-cluster | ✅ PASS (2026-07-12) | §T29 |
| **T30** `-p` push consistency | no-cluster | ✅ PASS (2026-07-12) | §T30 |
| **T31** scaleMetric webhook regression guard | cluster | ⏳ pending | §T31 — **use v0.17's `deploymentMode: "ModelMesh"` negative case; re-verify the normalization list is unchanged in v0.18** |
| **T32** ConfigMap + sample static `Standard` guard | no-cluster | ✅ PASS (2026-07-12) | §T32 |
| **T33** PYTHONPATH blocked-env-var validator guard | cluster | ⏳ pending | **NEW — full recipe below** |
| T26 podman build | — | ⏸ DEFERRED | needs podman host |

---

## Per-test details

Inherited-recipe tests (T01–T32) are documented in `0.17-test-report.md` / `0.16-test-report.md`; only v0.18 deltas get recorded here as each test runs. The one new test gets its full recipe:

### T01 — OLM bundle, default `kserve` ns

**Recipe:** `0.16-test-report.md` §T01 (as re-confirmed in `0.17-test-report.md` §T01). Executed literally, using the on-disk `p-kserve-operator-package/` rebuilt for the default-flow `v18001` image after Phase 4 left `v18002` (customer-registry) on disk.

**v0.18 result:** ✅ PASS — all assertions clean.

- Cluster reset, verified fresh via node `creationTimestamp` (`2026-07-11T18:04:02Z`, 4 default namespaces at 39s age).
- cert-manager **v1.17.2** installed + waited Ready.
- `operator-sdk olm install` — OLM v0.43.0 (latest stable, same as the v0.17 round — v0.29.0 assets are no longer downloadable from GitHub).
- Pull secret built from the already-`docker login`'d akashneha session via `kubectl create secret generic dockerhub-creds --from-file=.dockerconfigjson=$HOME/.docker/config.json --type=kubernetes.io/dockerconfigjson -n kserve-operator-system` (no PAT touched the CLI or chat — same discipline as [[feedback_credentials]]).
- `operator-sdk run bundle docker.io/akashneha/kserve-raw-operator-bundle:v18001 --namespace kserve-operator-system --install-mode SingleNamespace=kserve --pull-secret-name dockerhub-creds` — CSV `Succeeded` in ~17s.
- OperatorGroup `targetNamespaces: ["kserve"]` ✓.
- Operator pod `p-kserve-operator-controller-manager` image confirmed **`docker.io/akashneha/kserve-raw-operator:v18001`**, 1/1 Running, 0 restarts.
- `KServeRawMode` CR auto-created in `kserve`, reached `Ready` within ~30s.
- `kserve-controller-manager` pod 2/2 Running, 0 restarts.
- **ConfigMap confirmed:** `defaultDeploymentMode: "Standard"`; `storageInitializer.image = kserve/storage-initializer:v0.18.0`; ingress block `disableIstioVirtualHost=true`, `disableIngressCreation=true`, and the **new v0.18 key `disableHTTPRouteTimeout=false`** correctly present (confirms the Phase 1 finding that our parse-modify-reserialize ConfigMap patch preserves unknown/new upstream keys).
- Applied shipped `06-sample-model/sklearn-iris.yaml` (annotation `serving.kserve.io/deploymentMode: "Standard"`) to `default` ns — ISVC `Ready=True`.
- `kubectl get deploy,ksvc -n default`: predictor is a plain `Deployment` (`sklearn-iris-predictor`), `ksvc` resource type absent from the cluster entirely — confirms Standard/non-Knative mode, no Knative Serving installed.
- Inference via `kubectl port-forward` (no Host header, per the v0.17 lesson) → **`{"predictions":[1,1]}`** ✓ (2-record `iris-input.json`).
- 0 restarts on both operator and controller pods throughout.

**No deltas from the v0.17 recipe** — pure parity confirmed for this test.

---

### T02 — `install.sh` non-OLM default

**Recipe:** `0.16-test-report.md` §T02 (as re-confirmed in `0.17-test-report.md` §T02). Executed literally.

**v0.18 result:** ✅ PASS — all assertions clean.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-11T18:14:57Z`, 4 default ns at 43s age).
- cert-manager v1.17.2 installed + Ready.
- `cd p-kserve-raw && bash install.sh` (no env overrides → default `KSERVE_NAMESPACE=kserve`) — completed cleanly, all 4 stage files applied (CRDs, RBAC, core, runtimes).
- `kserve` ns auto-created by install.sh.
- **1 controller pod** in `kserve` ns (core only), 2/2 Running, 0 restarts.
- `kserve-operator-system` confirmed NotFound (non-OLM path, no operator installed).
- `KServeRawMode` CRD confirmed NotFound (only the operator package installs this CRD — install.sh's non-OLM path never touches it).
- **ConfigMap confirmed:** `defaultDeploymentMode: "Standard"`; `storageInitializer.image = kserve/storage-initializer:v0.18.0`.
- Sample iris applied to `default` ns (install.sh's own recipe, no `-n` flag) — Ready.
- Inference via port-forward → `{"predictions":[1,1]}` ✓.
- 0 restarts.

**No deltas from the v0.17 recipe.**

---

### T03 — `install.sh` with `KSERVE_NAMESPACE=servewell`

**Recipe:** `0.16-test-report.md` §T03 (as re-confirmed in `0.17-test-report.md` §T03).

**v0.18 result:** ✅ PASS — all 4 namespace-rewrite categories verified.

| # | Category | Evidence |
|---|---|---|
| 1 | `metadata.namespace` | `kserve-controller-manager` Deployment + 3 services (`-metrics-service`, `-service`, `kserve-webhook-server-service`) all in `servewell` ns |
| 2 | Webhook `clientConfig.service.namespace` | `MutatingWebhookConfiguration` (2 webhooks: `defaulter` + `pod-mutator`) + 5 `ValidatingWebhookConfiguration`s (clusterservingruntime, inferencegraph, inferenceservice, servingruntime, trainedmodel) all point at `ns=servewell` |
| 3 | Certificate `commonName` + `dnsNames` | `serving-cert` in `servewell`: `commonName=kserve-webhook-server-service.servewell.svc`; `dnsNames=["kserve-webhook-server-service.servewell.svc"]` |
| 4 | RoleBinding `subjects.namespace` | 3 bindings → `servewell`: 2 ClusterRoleBindings (`kserve-manager-rolebinding`, `kserve-proxy-rolebinding`) + 1 namespace-scoped `kserve-leader-election-rolebinding` |

- Controller 1/1 Running, 0 restarts.
- **ConfigMap confirmed `Standard`** in `servewell` ns.
- iris deployed to `default` (install.sh's own recipe) — Ready.
- Inference `{"predictions":[1,1]}` ✓.
- `kserve-operator-system` correctly absent (non-OLM path).

**Confirms:** Issue #1 fix (Python YAML-walk namespace rewrite in `install.sh`) continues to work correctly on v0.18. No deltas from v0.17.

---

### T04 — direct `kubectl apply -f operator-deployment.yaml` (no OLM, no install.sh)

**Recipe:** `0.16-test-report.md` §T04 (as re-confirmed in `0.17-test-report.md` §T04), executed literally including the canonical fixture step (pre-create `kserve` ns) rather than waiting through T21's multi-minute retry — T21 already covers that behavior as its own dedicated test.

**v0.18 result:** ✅ PASS.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-11T18:31:10Z`, 38s age).
- `kserve-operator-system` + `kserve` ns pre-created; `dockerhub-creds` pull secret built from the akashneha login (same non-PAT-on-CLI method as T01).
- On-disk `p-kserve-operator-package/` confirmed still the T01 default-flow (`v18001`) build — no rebuild needed for this test.
- `kubectl apply -f p-kserve-operator-package/operator-deployment.yaml` — CRD, ServiceAccount, 6 ClusterRoles + 1 Role, 1 RoleBinding + 2 ClusterRoleBindings, 1 metrics Service, 1 Deployment.
- Operator pod Ready in ~75s (image confirmed `docker.io/akashneha/kserve-raw-operator:v18001`).
- **Process note (honest, not a v0.18 regression):** I forgot to install cert-manager before this test. The CR correctly blocked on `CertManagerNotFound` → `ValidatingCertManager` for ~5.5 min, confirming the operator's pre-flight check works as designed (same lesson independently hit during the v0.17 round's T05). Installed cert-manager v1.17.2; CR proceeded through `InstallingCore` → `Ready` cleanly within ~2 min of cert-manager becoming Ready.
- `kserve-controller-manager` 2/2 Running, 0 restarts.
- iris in `default` Ready.
- Inference: `{"predictions":[1,1]}` ✓.
- 0 restarts on operator and controller pods.

**No deltas from the v0.17 recipe** (aside from the self-inflicted cert-manager process gap, which is not a code path difference).

---

### T05 — OLM bundle with custom `servewell` ns (apply.go rewrite path)

**Recipe:** `0.16-test-report.md` §T05 (v0.17's per-test detail was never filled in; recipe reconstructed from its summary-table row).

**v0.18 result:** ✅ PASS — all 4 apply.go rewrite categories confirmed.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-11T18:41:24Z`, 38s age).
- cert-manager v1.17.2 + OLM v0.43.0 installed (learned from T04 — didn't skip this time).
- `servewell` + `kserve-operator-system` ns pre-created; pull secret via non-PAT method.
- `operator-sdk run bundle docker.io/akashneha/kserve-raw-operator-bundle:v18001 --install-mode SingleNamespace=servewell ...` — CSV `Succeeded` ~16s.
- OperatorGroup `targetNamespaces: ["servewell"]` ✓; `WATCH_NAMESPACE` injected via OLM downward API (`valueFrom`) ✓.
- `KServeRawMode` CR auto-created in `servewell`, reached `Ready` in ~64s.

| # | Category | Evidence |
|---|---|---|
| 1 | ClusterRoleBinding `subjects.namespace` | `kserve-manager-rolebinding` + `kserve-proxy-rolebinding` → `servewell` |
| 2 | Certificate `commonName`+`dnsNames` | `kserve-webhook-server-service.servewell.svc` (both) |
| 3 | Webhook `clientConfig.service.namespace` | 2 mutating + 5 validating webhooks all → `servewell` |
| 4 | `inject-ca-from` annotation | `servewell/serving-cert` |

- `kserve-controller-manager` 2/2 Running, 0 restarts. Operator pod 0 restarts.
- **ConfigMap confirmed `Standard`** in `servewell`.
- iris in `default` Ready, inference `{"predictions":[1,1]}` ✓.

**No deltas from v0.17.**

---

### T06 — OLM `servewell` + external URL via nginx-ingress + `enable-ingress.sh`

**Recipe:** `0.16-test-report.md` §T06 (as re-confirmed in `0.17-test-report.md` §T06).

**v0.18 result:** ✅ PASS.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T05:22:13Z`, 27s age).
- cert-manager v1.17.2 + OLM v0.43.0 + nginx-ingress `controller-v1.12.1` all installed clean.
- `servewell` + `kserve-operator-system` ns pre-created; bundle deployed (`v18001`) — CSV Succeeded ~16s, CR Ready ~108s.
- `KSERVE_NS=servewell bash enable-ingress.sh` — patched ConfigMap (`ingressClassName=nginx`, `disableIngressCreation=false`), restarted controller, **built-in Issue #7 webhook probe (port 9443, `kubectl run curlimages/curl`) ran and confirmed the webhook responsive** — script's `kubectl attach` hit the known Docker Desktop streaming quirk (`http: server gave HTTP response to HTTPS client`) and fell back to log-streaming, exactly as in the v0.17 round, still reported success correctly.
- ConfigMap confirmed: `ingressClassName: "nginx"`, `disableIngressCreation: false`.
- Applied iris ISVC immediately after `enable-ingress.sh` — **no `context deadline exceeded`** (the built-in probe avoided the webhook race that the raw v0.16 recipe originally hit before the Issue #7 fix landed).
- ISVC Ready.
- KServe created an `Ingress` object: `class=nginx`, hosts `sklearn-iris-default.example.com` + `sklearn-iris-predictor-default.example.com`, backend `sklearn-iris-predictor:80`.
- **ConfigMap confirmed `Standard`** for `defaultDeploymentMode`.
- External curl via `kubectl port-forward` to `ingress-nginx-controller:80` with `Host: sklearn-iris-default.example.com` → `{"predictions":[1,1]}` ✓.

**No deltas from v0.17.** Confirms Issue #7's baked-in webhook probe in `enable-ingress.sh` continues to work correctly on v0.18 — no manual rollout-wait workaround needed.

---

### T07 — PVC-based offline iris (Part C — no LocalModelCache)

**Recipe:** `0.16-test-report.md` §T07 (as re-confirmed in `0.17-test-report.md` §T07).

**v0.18 result:** ✅ PASS.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T05:29:27Z`, 35s age).
- `kubectl exec` smoke-tested and confirmed still broken this session (Docker Desktop streaming bug: `http: server gave HTTP response to HTTPS client`) — used the established Job (self-download) workaround instead of `kubectl cp`/`exec`. `kubectl logs` confirmed still working (used to verify the download).
- Standard prereqs: cert-manager v1.17.2 + OLM v0.43.0 + bundle (`v18001`) into `kserve` ns — CSV Succeeded, CR Ready ~43s, controller 2/2 Running 0 restarts.
- PVC `offline-models-pvc` (1Gi, RWO) Bound immediately (hostpath storage class).
- Model side-loaded via a self-downloading `curlimages/curl` Job (not `kubectl cp`) — downloaded `model.joblib` (5408 bytes, matches recipe exactly) directly into the PVC mount, confirmed via `kubectl logs job/model-loader`.
- ISVC `sklearn-iris-pvc` with `storageUri: pvc://offline-models-pvc/sklearn/iris/1.0/model` + `Standard` annotation — Ready.
- **ConfigMap confirmed `Standard`**.
- Predictor pod 1/1 Running, 0 restarts, no Init errors — storage-initializer mounted the PVC and found the model without any egress at predictor startup.
- Inference via `kubectl port-forward` (worked fine, no exec/attach dependency) → `{"predictions":[1]}` ✓ (single-record request, matches recipe).
- 0 restarts on both controller and predictor.

**No deltas from v0.17.** The `pvc://` storage-initializer URI scheme continues to work identically on v0.18.

---

### T08 — `--cert` build-time CA injection (build-only)

**Recipe:** `0.16-test-report.md` §T08 + §T08-RETEST (Issue #8 fix). No-cluster, throwaway tag `v18008`.

**v0.18 result:** ✅ PASS. No cluster reset needed (ran in parallel with T07's cluster still up).

- Generated a throwaway CA (`CN=kserve-op-test-ca-T08-v18`).
- `./generate-kserve-operator.sh -c p-kserve-operator && ./generate-kserve-operator.sh -t p-kserve-operator -m github.com/akashdeo/p-kserve-operator -d akashdeo.com -s p-kserve-raw -i docker.io/akashneha/kserve-raw-operator:v18008 --install-mode SingleNamespace --cert /tmp/test-ca-t08-v18.crt -b` — **exit 0**.
- Issue #8 fix confirmed still working: "Skipping image push (pass -p to push automatically)." printed, script proceeded past the push block to `[6/6] Generating Standalone Deployment Package` — **no exit-1 regression**.
- Generated `p-kserve-operator/Dockerfile` contains `COPY test-ca-t08-v18.crt /usr/local/share/ca-certificates/test-ca-t08-v18.crt` + `RUN update-ca-certificates`.
- `docker build --target builder` rebuilt the builder stage cleanly.
- Builder stage's `/etc/ssl/certs/ca-certificates.crt` decoded via `openssl crl2pkcs7`/`pkcs7 -print_certs` — **exact CN match**: `CN=kserve-op-test-ca-T08-v18`.
- Dockerfile confirmed multi-stage (`FROM golang:1.24 AS builder` → `FROM gcr.io/distroless/static:nonroot`) — the CA lands only in the builder stage by construction, not the final runtime image.

**No deltas from v0.16/v0.17.**

---

### T09 — `--cert` + `--customer-registry` combined (build-only)

**Recipe:** `0.16-test-report.md` §T09. No-cluster, throwaway tag `v18009`.

**v0.18 result:** ✅ PASS.

- `-b -o` (build + OLM bundle, no push) with both `--cert` and `--customer-registry docker.io/akashdeohuf` — **exit 0**, reached the OLM bundle block, deploy instructions correctly printed `docker.io/akashdeohuf/kserve-raw-operator-bundle:v18009`.
- Dockerfile CA injection present (same as T08).
- `operator-deployment.yaml` image rewritten to `docker.io/akashdeohuf/kserve-raw-operator:v18009` — customer-registry manifest rewrite confirmed.
- Bundle CSV (`p-kserve-operator/bundle/manifests/p-kserve-operator.clusterserviceversion.yaml`) also rewritten to `akashdeohuf` — confirms the rewrite reaches both the raw deployment YAML and the OLM bundle CSV.
- Local Docker images correctly tagged `akashneha/kserve-raw-operator:v18009` + `-bundle:v18009` (build target unchanged — `--customer-registry` only rewrites manifest *text*, not the actual build/push target, consistent with the documented v18002 behavior from Phase 4).
- Bundle built locally (no `-p` given) — no push attempted.

**No deltas from v0.16/v0.17.** Confirms the flags don't conflict at the build layer and both rewrite paths (raw manifest + bundle CSV) work together with `--cert`.

---

### T27 — Combined `--cert` + `--http-proxy` + `--https-proxy` + `--no-proxy` Dockerfile patching

**Recipe:** `0.16-test-report.md` §T27. No-cluster, no docker build (`echo n |` skips the push prompt path; no `-b`).

**v0.18 result:** ✅ PASS — all 5 assertions clean.

- Throwaway CA + `--cert` + all 3 proxy flags, no `-b` (Dockerfile-patch-only, no real build against the fake proxy URL).
- (1) Cert `COPY` + `RUN update-ca-certificates` present ✓
- (2) All 6 proxy `ENV` directives present (`HTTP_PROXY`/`HTTPS_PROXY`/`NO_PROXY` × upper+lower) ✓
- (3) URL values preserved verbatim ✓
- (4) `ENV *PROXY` lines scoped to `golang:1.24` builder stage ONLY — zero occurrences under `distroless` ✓
- (5) Build correctly skipped — no `v18027` image in `docker images` ✓

**No deltas from v0.16/v0.17.**

---

### T28 — Proxy flag all-or-nothing validation rejection

**Recipe:** `0.16-test-report.md` §T28. No-cluster, 5 sub-cases.

**v0.18 result:** ✅ PASS — all 5 sub-cases clean.

| Sub-case | Expected | Actual |
|---|---|---|
| (a) only `--http-proxy` | exit 1, "must be supplied together" | ✓ |
| (b) only `--no-proxy` | exit 1, same message | ✓ |
| (c) `--http-proxy`+`--https-proxy`, missing `--no-proxy` | exit 1, same message | ✓ |
| (d) all three together | validation passes (0 occurrences of the error message); confirmed via log — proxy ENV successfully patched into Dockerfile, the eventual exit 1 came from unrelated garbage test args (`-m foo -d bar`) failing downstream at the Go scaffold/build stage, not from proxy validation | ✓ |
| (e) zero proxy flags + `--help` | exit 0, normal help, no false-positive validation error | ✓ |

**No deltas from v0.16/v0.17.**

---

### T29 — Bundle naming derivation (`-bundle` in image name, not tag)

**Recipe:** `0.16-test-report.md` §T29. No-cluster, pure bash logic.

**v0.18 result:** ✅ PASS — all 4 checks clean.

- (a) Normal `registry/path/image:tag` input → `-bundle` inserted before the tag ✓
- (b) Port-colon edge case `localhost:5000/img:v1` → port not confused with tag (last colon wins) → `localhost:5000/img-bundle:v1` ✓
- (c) No-tag input `registry/img` → `registry/img-bundle`, no spurious `:` ✓
- (d) Invariant: `BUNDLE_IMG="${IMAGE_TAG%:*}-bundle:${IMAGE_TAG##*:}"` present in script source ✓

**No deltas from v0.16/v0.17.** Guards against a future refactor reverting the OperatorHub-convention naming (`f03bc04`).

---

### T30 — `-p` flag controls bundle push consistently with operator push

**Recipe:** `0.16-test-report.md` §T30. No-cluster, 4 static assertions on `container_build_singlearch_flat`.

**v0.18 result:** ✅ PASS — all 4 assertions clean.

- (1) Wrapper checks `${AUTO_PUSH}` — `-p` consistency (`0094696`) guard ✓
- (2) "built locally. Pass -p to also push" message present ✓
- (3) Wrapper uses plain `docker push`, NOT `buildx --push` — `NO_PROXY` honored (`cfa53bb`) ✓
- (4) Wrapper uses `buildx build ... --load` (materializes into daemon before push) ✓

**No deltas from v0.16/v0.17.**

---

### T32 — ConfigMap default + shipped sample use `"Standard"` (no-cluster static guard)

**Recipe:** `0.16-test-report.md` / `0.17-test-report.md` §T32. No-cluster, 4 static assertions on `p-kserve-raw/` (regenerated at Phase 2).

**v0.18 result:** ✅ PASS — 4/4 assertions.

- `"defaultDeploymentMode": "Standard"` present (1) ✓
- `RawDeployment` in ConfigMap absent (0) ✓
- Sample annotation `Standard` present (1) ✓
- `RawDeployment` sample annotation absent (0) ✓

**No deltas from v0.16/v0.17.**

---

### T10 — Customer-registry archive+load → `akashdeohuf`, default `kserve` ns, in-cluster URL

**Recipe:** `0.16-test-report.md` §T10 (as re-confirmed in `0.17-test-report.md` §T10). Images already built+pushed to `akashneha` in Phase 4 (no rebuild needed) — this test only rebuilds the customer-registry-rewritten package, archives, loads, and deploys from `akashdeohuf`.

**v0.18 result:** ✅ PASS (with a reproduction of a known, already-documented upstream finding — see below).

- Package regenerated with `--customer-registry docker.io/akashdeohuf -o` (no `-b`/`-p`, image already exists from Phase 4) — `operator-deployment.yaml` AND the bundle CSV both confirmed rewritten to `docker.io/akashdeohuf/kserve-raw-operator:v18002`.
- `mirror-images.sh --archive` — produced `images/operator.tar` (~32MB) + `images/bundle.tar` (~15KB), matching recipe expectations.
- `mirror-images.sh --load` (no `--user`/`--pass`) crashed silently: `read -rp` under `set -e` dies on non-interactive stdin — **same bug class as the `generate-kserve-operator.sh` Issue #8 fix, but in `mirror-images.sh`, which never got the equivalent fix.** Not previously hit because prior rounds always supplied `--user`/`--pass` explicitly.
- Retried by piping blank input to bypass the crash (letting skopeo fall back to ambient `docker login` credentials, no explicit `--dest-creds`) — reproduced the **exact same Docker Hub cross-namespace blob-mount bug** documented in the v0.17 round's T10: `insufficient_scope` / unparseable combined-scope challenge (`repository:akashdeohuf/...:pull,push repository:akashneha/...:pull`) when Docker Hub proposes a cross-repo blob mount for byte-identical content. **Confirms this is a persistent upstream Docker Hub quirk, not version-specific.**
- **Fix (same as v0.17):** explicit `skopeo copy --dest-creds akashdeohuf:<PAT>` for both images, run directly by the user (PAT never touched me — hard credentials-policy line). Both loads succeeded.
- Verified via `skopeo inspect`: `akashdeohuf/kserve-raw-operator:v18002` ✓, `akashdeohuf/kserve-raw-operator-bundle:v18002` ✓ (bundle digest identical to the akashneha original — pure mirror; operator digest differs slightly due to archive-copy manifest re-wrapping, not a content difference).
- Cluster prereqs: cert-manager v1.17.2 + OLM v0.43.0 (fresh install this round). `kserve` + `kserve-operator-system` ns pre-created; pull secret built from the (now akashdeohuf-authenticated) local docker config.
- `operator-sdk run bundle docker.io/akashdeohuf/kserve-raw-operator-bundle:v18002 --install-mode SingleNamespace=kserve` — CSV Succeeded.
- **Operator pod image confirmed `docker.io/akashdeohuf/kserve-raw-operator:v18002`** — pulled from the customer registry, not the build registry.
- `KServeRawMode` CR auto-created in `kserve`, Ready ~46s. Controller 2/2 Running, 0 restarts.
- **ConfigMap confirmed `Standard`**.
- iris Ready, inference `{"predictions":[1,1]}` ✓.

**New finding (not a project regression):** `mirror-images.sh`'s interactive-`read`-under-`set -e` crash on non-interactive stdin is a genuine latent bug, same class as the already-fixed Issue #8 in `generate-kserve-operator.sh`. Worth a follow-up fix (mirror `generate-kserve-operator.sh`'s pattern: skip the prompt when running non-interactively, or add a `[[ -t 0 ]]` TTY check with a clear error). Filed as a doc/backlog item, not blocking this round since the explicit `--user`/`--pass` (or raw `skopeo --dest-creds`) path works.

**No deltas from v0.17** in the actual KServe deploy behavior — both findings (mirror-images.sh read crash, Docker Hub blob-mount bug) are pre-existing/upstream issues, reproduced identically, not introduced by the v0.18 bump.

---

### T11 — Customer-registry + `deploy-bundle.sh` helper, default ns

**Recipe:** `0.16-test-report.md` §T11 (as re-confirmed in `0.17-test-report.md` §T11). Reused the `akashdeohuf` images already loaded in T10.

**v0.18 result:** ✅ PASS.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T06:12:25Z`, 21s age).
- cert-manager v1.17.2 + OLM v0.43.0 installed. `kserve` + `kserve-operator-system` ns pre-created; pull secret from ambient akashdeohuf login.
- `echo "A" | bash deploy-bundle.sh dockerhub-creds` — Option A (OLM) branch.
- `BUNDLE_IMAGE` correctly hardcoded to `docker.io/akashdeohuf/kserve-raw-operator-bundle:v18002` in the script.
- All 3 critical flags confirmed correct: `--namespace=kserve-operator-system`, `--install-mode=SingleNamespace=kserve`, pull-secret arg passed through.
- Trailing success message `"Operator installed via OLM into 'kserve-operator-system'."` printed only after real CSV `Succeeded` (gated by `set -e`, same discipline as the `89edc6f` fix).
- OperatorGroup `targetNamespaces: ["kserve"]` ✓.
- **Operator pod image confirmed `docker.io/akashdeohuf/kserve-raw-operator:v18002`.**
- `KServeRawMode` CR Ready, controller 2/2 Running, 0 restarts.
- iris Ready, inference `{"predictions":[1,1]}` ✓.

**No deltas from v0.16/v0.17.** Confirms the `deploy-bundle.sh` Option A fix continues to work correctly on v0.18.

---

### T12 — Customer-registry + custom `servewell` ns + external URL (combined stress)

**Recipe:** `0.16-test-report.md` §T12 (as re-confirmed in `0.17-test-report.md` §T12). Three layers stacked: customer registry + Design D namespace rewrite + external URL via nginx-ingress.

**v0.18 result:** ✅ PASS — no webhook race, all three layers confirmed.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T06:24:51Z`, 35s age).
- cert-manager v1.17.2 + OLM v0.43.0 + nginx-ingress all installed clean.
- Bundle deployed from `akashdeohuf` into `servewell` — CSV Succeeded, CR Ready ~56s.

| Layer | Field | Result |
|---|---|---|
| Customer registry | Operator pod image | `docker.io/akashdeohuf/kserve-raw-operator:v18002` ✓ |
| Design D namespace | Certificate `serving-cert` dnsNames | `["kserve-webhook-server-service.servewell.svc"]` ✓ |
| Design D namespace | `KServeRawMode` CR ns | `servewell`, Ready ✓ |
| External URL | `enable-ingress.sh` webhook probe | **responsive after attempt 1/30** — no `kubectl attach` quirk this run (session-to-session variance, same subsystem T06 hit) |
| External URL | ConfigMap ingress block | `ingressClassName: nginx`, `disableIngressCreation: false` ✓ |
| External URL | Ingress object | `class=nginx`, correct hosts, `ADDRESS=localhost` (Docker Desktop populates this — unlike kind clusters, environmental detail not a functional issue) |

- Immediate ISVC apply after `enable-ingress.sh` — **no `context deadline exceeded`** (Issue #7's built-in probe avoided the webhook race that the original v0.16 recipe needed a `sleep 20` workaround for; T06+T12 both clean this round without any manual wait).
- Controller 2/2 Running, 0 restarts.
- External curl via nginx (Host header) → `{"predictions":[1]}` ✓ (single-record request).

**No deltas from v0.16/v0.17.** Confirms Issue #7's webhook probe continues to eliminate the race under the hardest 3-layer combination on v0.18.

---

### T17 — `KServeRawMode` CR delete UX (confirms documented gap)

**Recipe:** `0.16-test-report.md` §T17 (as re-confirmed in `0.17-test-report.md` §T17). OLM default flow (`v18001`) baseline.

**v0.18 result:** ✅ PASS — documented gap reproduces exactly as expected.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T06:33:32Z`).
- Rebuilt v18001 default-flow package (on disk was still v18002 customer-registry from T10-T12).
- cert-manager + OLM + bundle deploy — CR Ready baseline established.
- `kubectl delete kserverawmode kserve-rawmode -n kserve` — CR gone within seconds, no finalizer hang.
- **90s observation window: `No resources found` throughout — zero auto-recreation.** Documented gap reproduces.
- Operator pod: **same identity + same `startTime` (`2026-07-12T06:36:46Z`), 0 restarts** — confirms this is a silent no-op, not a crash/restart.
- `kserve-controller-manager` unaffected, still Running (not owned by the deleted CR).
- CRD `kserverawmodes.operator.akashdeo.com` intact.
- New ISVC (`sklearn-iris`) still deploys and reaches Ready post-delete — KServe itself keeps functioning even though the operator no longer reconciles.

**No deltas from v0.16/v0.17.** The documented "deleted CR is not auto-recreated" gap in `gaps-and-observations.md` continues to hold on v0.18, unchanged.

---

### T19 — `install-operator-deployment.sh` default flow (no env vars) + Design D iris deploy

**Recipe:** `0.16-test-report.md` §T19 (as re-confirmed in `0.17-test-report.md` §T19).

**v0.18 result:** ✅ PASS — all 5 assertions clean.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T06:57:58Z`, 54s age). Reused v18001 default-flow package from T17.
- **Byte-equivalence:** `install-operator-deployment.sh --dry-run` output vs `operator-deployment.yaml` — **empty diff, exit 0** (no env vars → passthrough short-circuit confirmed).
- cert-manager v1.17.2 + OLM v0.43.0 installed; namespaces pre-created; pull secret from ambient login.
- `bash install-operator-deployment.sh` applied cleanly — CRD, ServiceAccount, RBAC, Deployment all created.
- `kubectl wait --for=condition=Ready kserverawmode/...` timed out — **pre-existing recipe imprecision**, not a functional issue: this CRD uses `.status.phase`, not `.status.conditions[]`, so the `--for=condition=Ready` syntax never matches. Direct `.status.phase` check confirmed `Ready` (~5m12s).
- **Pull secret confirmed ONLY in `kserve-operator-system`** (Design D footprint — not in `default`).
- iris deployed to `default` workload ns (Design D #3, NOT the `kserve` runtime-control ns) — Ready.
- **`storage-initializer` init container confirmed injected** on the predictor pod — Design D correctly avoids upstream's `control-plane DoesNotExist` webhook exclusion that would skip injection in the `kserve` ns.
- Inference `{"predictions":[1]}` ✓ (matches recipe exactly).
- 0 restarts on both operator and controller.

**No deltas from v0.16/v0.17.** Design D three-namespace model continues to work correctly on v0.18.

---

### T20 — Three-way flagship: `OPERATOR_NAMESPACE` + `KSERVE_NS` + `KSERVE_WORKLOAD_NS`

**Recipe:** `0.16-test-report.md` §T20 (as re-confirmed in `0.17-test-report.md` §T20). The Design D flagship — one generated package, 3 independently-chosen namespaces, zero rebuild.

**v0.18 result:** ✅ PASS — all 9 pass criteria confirmed.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T07:08:40Z`, 19s age). Reused v18001 package.
- `ops-custom` + `servewell` + `team-fraud` pre-created; pull secret built (non-PAT method) in exactly `ops-custom` + `team-fraud`, replicating `setup-credentials.sh SYSTEM_NS=ops-custom KSERVE_WORKLOAD_NS=team-fraud --also-workload-ns`'s documented footprint.
- `OPERATOR_NAMESPACE=ops-custom KSERVE_NS=servewell bash install-operator-deployment.sh` — applied cleanly.
- `kubectl get ns kserve-operator-system` → **NotFound** (wrapper correctly renamed away) ✓
- Operator pod Running in `ops-custom` ✓
- `WATCH_NAMESPACE` env var = **literal `servewell`** (Option B substitution, not OLM downward API) ✓
- `KServeRawMode` CR Ready in `servewell` (NOT `kserve`, NOT `team-fraud`) in ~4m21s ✓
- `kserve-controller-manager` Running in `servewell` ✓
- iris deployed to `team-fraud` (the workload ns) — Ready, **`storage-initializer` confirmed injected** ✓
- Inference `{"predictions":[1]}` ✓
- **Pull secret confirmed in exactly 2 namespaces** (`ops-custom` + `team-fraud`) — no leak to `default`, `servewell`, or `kserve-operator-system` ✓
- 0 restarts on operator and controller.

**No deltas from v0.16/v0.17.** All three Design D namespaces remain independently configurable at deploy time on v0.18 — one package, any topology, zero rebuilds.

---

### T21 — Indefinite auto-init retry (Follow-up B)

**Recipe:** `0.16-test-report.md` §T21 (as re-confirmed in `0.17-test-report.md` §T21). **Critical:** do NOT pre-create `kserve` ns.

**v0.18 result:** ✅ PASS — all 4 pass criteria confirmed.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T07:17:58Z`, 28s age). Process note: forgot cert-manager on the first attempt (recipe explicitly requires it as a pre-state) — installed it, retried cleanly.
- `kserve-operator-system` ns created ONLY; operator deployed via `install-operator-deployment.sh` before `kserve` ns existed.
- **Exact exponential backoff confirmed via logs**: `2s→4s→8s→16s→30s`, then steady 30s across attempts 1–9 during the 2-min+ wait — well past the old 5-attempt cap.
- **Zero "exhausted retries"/"gave up" message.**
- Operator pod stayed Running throughout, 0 restarts.
- `kubectl create namespace kserve` → next retry (attempt 10, 30s after attempt 9) succeeded: `Auto-init succeeded {"attempts": 10}` logged.
- **CR reached Ready with NO manual `kubectl rollout restart`** — fully self-healed.
- Same operator pod identity throughout (`p-kserve-operator-controller-manager-799ddfd7c5-nv4fq`), 0 restarts.
- Sanity: iris deployed and reached Ready post-recovery, confirming the operator is fully functional after the delayed auto-init.

**Process note (session-management, not a functional issue):** a `kubectl get kserverawmode` AGE column briefly read "30m" for a CR that should have been only minutes old — investigated immediately per the T07 false-PASS lesson discipline. Verified genuine via `creationTimestamp` cross-check: CR created `07:21:58Z`, node created `07:17:58Z` (same fresh cluster, ~4 min gap consistent with cert-manager install + 2-min backoff wait), current wall-clock had simply advanced ~31 min during the test's tool-call/wait overhead. Not stale state — same cluster throughout.

**No deltas from v0.16/v0.17.** The Follow-up B indefinite-retry refactor (`95ffc69`) continues to eliminate the old 5-attempt cap on v0.18; recovery remains bounded by backoff (max 30s), not attempt count.

---

### T22a — `setup-credentials.sh` default 1-ns footprint

**Recipe:** `0.16-test-report.md` §T22a (as re-confirmed in `0.17-test-report.md` §T22a). Fake test credentials (placement-only test, not real registry auth).

**v0.18 result:** ✅ PASS — all 5 pass criteria confirmed.

- Cluster reset, verified fresh (node `creationTimestamp` `2026-07-12T07:54:48Z`, 16s age). cert-manager + OLM installed (so `olm`/`operators` ns exist for the negative checks).
- `kserve-operator-system` + `kserve` ns pre-created.
- `bash setup-credentials.sh --user fake-user --pass fake-pass` (default flow, no `--customer-registry`, no `--also-workload-ns`) — exit 0.
- Exact expected message: `"✅ Pull secret 'dockerhub-creds' created in: kserve-operator-system."` + workload-ns-skipped note.
- Secret confirmed present ONLY in `kserve-operator-system`; confirmed absent in `default`, `olm`, `operators` (4 explicit checks).

**No deltas from v0.16/v0.17.**

---

### T22b — `setup-credentials.sh` multi-workload-ns (3-ns footprint)

**Recipe:** `0.16-test-report.md` §T22b (as re-confirmed in `0.17-test-report.md` §T22b). Fake test credentials, 2 sub-cases.

**v0.18 result:** ✅ PASS — both sub-cases clean, all 6 pass criteria confirmed.

**Sub-case 1 (typo fail-fast):** `KSERVE_WORKLOAD_NS=team-typo-does-not-exist bash setup-credentials.sh --also-workload-ns` (namespace intentionally not created) → **exit 1**, error correctly names the typo'd namespace (`namespace 'team-typo-does-not-exist' — workload ns required...`) with the exact `kubectl create namespace team-typo-does-not-exist` fix suggestion.

**Sub-case 2 (happy path):** After `kubectl create ns kserve team-a team-b`, ran `KSERVE_WORKLOAD_NS=team-a,team-b bash setup-credentials.sh --also-workload-ns` → **exit 0**. Pre-flight logged both `✅ workload namespace 'team-a' exists` / `'team-b' exists`. Secret created in all 3 target namespaces; exact summary line `"✅ Pull secret 'dockerhub-creds' created in: kserve-operator-system team-a team-b."`. Verified secret **present** in `kserve-operator-system`+`team-a`+`team-b`, **absent** in `default`+`kserve`.

**No deltas from v0.16/v0.17.** Confirms Design D's multi-tenant onboarding pattern continues to work correctly on v0.18.

---

### T31/T33 (remaining)

⏳ pending — cluster reset requested before each. Last 2 tests in the round.

### T33 — PYTHONPATH blocked-env-var validator guard (NEW in v0.18)

**Purpose:** Lock in the new v0.18 admission validator (`pkg/validation/env_policy.go`, `DefaultBlockedEnvVars = ["PYTHONPATH"]`). Same shape as T31 (scaleMetric): a negative + positive pair that fails loudly if upstream ever removes or loosens the validator, keeping the Part D Troubleshooting doc from rotting silently. Cluster test, ~5 min.

**Source anchor (verified Phase 1, v0.18 tree):**
- `pkg/validation/env_policy.go:38` — error `setting %s in container %q is not allowed for security reasons`.
- `pkg/apis/serving/v1beta1/inference_service_validation.go:200` — `validateBlockedEnvVars(isvc)` iterates predictor/transformer/explainer containers + InitContainers + WorkerSpec.

**Recipe:**
1. Cluster reset (verify fresh via node `creationTimestamp`).
2. Install KServe (default flow, `v18001` OLM bundle or `install.sh` — either exercises the same validating webhook). Wait controller `2/2 Running`, then confirm the webhook HTTPS server on 9443 is actually listening (in-cluster curl probe, per [[enable-ingress-webhook-probe]]) before applying — a Ready pod ≠ Ready webhook.
3. **NEGATIVE case:** `kubectl apply` an ISVC in `default` ns whose predictor sets `PYTHONPATH` on a container, e.g.:
   ```yaml
   apiVersion: serving.kserve.io/v1beta1
   kind: InferenceService
   metadata:
     name: t33-pythonpath-neg
     annotations:
       serving.kserve.io/deploymentMode: "Standard"
   spec:
     predictor:
       containers:
         - name: kserve-container
           image: kserve/sklearnserver:v0.18.0
           env:
             - name: PYTHONPATH
               value: /custom
   ```
   → expect **exit ≠ 0**, stderr containing `not allowed for security reasons` (and naming `PYTHONPATH` + the container).
4. **POSITIVE case:** same ISVC without the `PYTHONPATH` env var (drop the whole `env:` block, or use the standard `sklearn-iris` storageUri form) → expect **exit 0**, `READY=True` within 2 min.
5. `kubectl get isvc t33-pythonpath-pos` shows `READY=True`; curl the positive ISVC → `{"predictions":[1]}` (single-instance payload, like T31).

**Pass criteria:** negative case rejected at admit time with the exact `not allowed for security reasons` message; positive case Ready + inference succeeds. Cross-links to Part D Troubleshooting (Phase 6) + `kserve-version-comparison.md` § v0.17→v0.18 downstream compatibility (Phase 6) + [[kserve-017-to-018-pythonpath-env-block]] memory.

---

## Carry-forward doc fixes queued for this round

- **QUICK_START.md test-report cross-links** (lines ~31, ~50) still point at `0.17-test-report.md`. Update to `0.18-test-report.md` once this report is validated (deferred from the retarget commit `813c310`, mirroring the v0.17 round's discipline of not touching test-report references in the pure version-bump commit). Line ~31 also still says "16-test" — correct to "26-test" at the same time.
- The two v0.17 doc inconsistencies logged in `0.17-test-report.md` (pull-secret name `docker-pull-secret` vs `dockerhub-creds` in the package README template; `setup-credentials.sh --help` missing the default secret name) carry forward if not yet fixed — re-check during T11/T22a.

*Per-test details for T01–T32 append as each test completes.*
