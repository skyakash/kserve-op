# Ignore generated deployment directories
*-kserve-raw/
*-kserve-operator/
*-kserve-operator-package/
bundle-*/

# Ignore local KServe source repository (auto-extracted by generate-kserve-raw.sh from the bundled zip)
kserve-source/
kserve-source.*/
kserve-master/

# Transient probe output from generate-kserve-raw.sh runs against throwaway targets (e.g. -t ./tmp/...)
tmp/

# Local version-comparison workspace (analysis only, not for commit)
kserve-diff/

# Temporary test artifacts
*.log
inference_result.json
test-kserve-installation.yaml

# System files
.DS_Store
.claude/
